@isTest
private class Test_HL_EET {
    @isTest private static void TestBasicFunctionality(){
        HL_EET.GetByRecordTypeId(null, null);
        HL_EET.GetByRole(null, null);
        HL_EET.GetByRecordTypeIdFRExt(null, null);
        HL_EET.GetByRole(null, null);
        
    }
}